# Backend Encore Secrets Configuration
# DO NOT commit actual secrets to version control
# Use `encore secret set --type dev <SecretName>` to configure secrets

# =============================================================================
# REQUIRED SECRETS FOR PRODUCTION
# =============================================================================
# These secrets must be configured via Encore Cloud dashboard or CLI

# OpenAI API Key
# Get from: https://platform.openai.com/api-keys
# Used for: AI chat assistant, content generation, product recommendations
# Cost: ~$20-50/month with rate limiting
OpenAIKey=your-openai-api-key-here

# Google Sheets Integration
# Get from: Google Cloud Console (https://console.cloud.google.com)
# Create a service account and download credentials JSON
# Used for: Content ingestion from Google Sheets
GoogleSheetsId=your-google-sheets-id
GoogleClientEmail=your-service-account@project.iam.gserviceaccount.com
GooglePrivateKey=your-google-private-key-here

# Amazon Product Advertising API
# Get from: Amazon Associates Program (https://affiliate-program.amazon.com)
# Register for PA-API access
# Used for: Product search, sync, affiliate links
AmazonAccessKey=your-amazon-access-key
AmazonSecretKey=your-amazon-secret-key
AmazonStoreId=your-amazon-store-id

# WordPress Publishing
# Get from: Your WordPress site (Application Password)
# Used for: Cross-posting articles to WordPress
WordPressUrl=https://your-wordpress-site.com
WordPressUsername=your-wordpress-username
WordPressPassword=your-application-password

# Medium Publishing
# Get from: Medium Settings > Integration tokens
# Used for: Cross-posting articles to Medium
MediumToken=your-medium-integration-token

# =============================================================================
# SETTING SECRETS VIA ENCORE CLI
# =============================================================================
# Development environment:
#   echo "your-secret-value" | encore secret set --type dev SecretName
#
# Production environment (via Encore Cloud):
#   echo "your-secret-value" | encore secret set --type production SecretName
#
# Example:
#   echo "sk-abc123..." | encore secret set --type dev OpenAIKey

# =============================================================================
# LOCAL DEVELOPMENT NOTES
# =============================================================================
# - All secrets can use placeholder values for local development
# - External integrations won't work with placeholder values
# - Set real API keys to test integrations locally
# - Never commit real API keys to version control
# - Use .gitignore to prevent accidental commits

# =============================================================================
# COST CONSIDERATIONS
# =============================================================================
# OpenAI API: ~$20-50/month (set billing limits in OpenAI dashboard)
# Google Sheets: FREE (sufficient quota for this use case)
# Amazon PA-API: FREE (requires Associates account in good standing)
# WordPress: FREE (self-hosted or wordpress.com)
# Medium: FREE
#
# Total estimated cost: $20-50/month
