{
  "name": "backend",
  "version": "1.0.0",
  "type": "module",
  "packageManager": "bun",
  "scripts": {
    "build": "cd ../frontend && bun install && bun x vite build --outDir=../backend/frontend/dist"
  },
  "dependencies": {
    "encore.dev": "^1.52.3",
    "googleapis": "^149.0.0"
  },
  "devDependencies": {
    "typescript": "^5.9.2"
  }
}