# Production Environment Variables for Netlify Deployment
# Copy this file and configure the values in Netlify UI:
# Site settings > Environment variables

# Backend API URL - Point to your Encore Cloud production endpoint
# Format: https://production-<app-name>.encr.app
# Or your custom domain if configured in Encore
VITE_CLIENT_TARGET=https://production-pure-living-pro-4mhi.encr.app

# Alternative staging URL for testing
# VITE_CLIENT_TARGET=https://staging-pure-living-pro-4mhi.encr.app

# Note: This is already configured in netlify.toml
# You can override it in Netlify UI if needed
